# Phase 11: Auth & Charts

## Overview
Phase 11 improved the visual representation of data and added initial mock-auth states.

## Key Changes
- **Recharts Integration**: Implemented `recharts` to render the `VitalsChart` component, plotting step counts and heart rates over time.
- **Auth Shell**: Built a basic mock login/auth context to simulate a secure environment and conditionally render the main application shell.
