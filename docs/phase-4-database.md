# Phase 4: Database Setup

## Overview
Phase 4 introduced persistent data storage using PostgreSQL and Drizzle ORM.

## Key Changes
- **Drizzle ORM**: Installed and configured Drizzle ORM within the `api-gateway`.
- **Database Schema**: Defined the core relational schemas (`users`, `patients`, `vitalsLogs`, `voiceLogs`).
- **Seed Scripts**: Created `seed.ts` to populate the database with realistic mock data for development and testing.
